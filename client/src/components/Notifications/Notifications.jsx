import React from "react"
import styles from "./Notifications.module.css"

const Notifications = (props) => {
  return <main className={styles.main}>Notifications</main>
}

export default Notifications
